class Person {
    // 생성자
    constructor (name) {
        this.name = name;
    }
}

console.dir(Person);

function Person1(name) {
    // 인스턴스 생성 및 초기화
    this.name = name;
}
console.dir(Person1);

